// Crio a interface Usuario
// Qualquer objeto ou classe que implemente/use esta interface PRECISA seguir as regras dela, ou seja, ter todos os seus atributos, tipos e metodos(se ter)
export interface IUsuario {
    nome: string
    idade:number
}